# Bundled font licenses

No general document font files are bundled in this development build yet.

Every font added here must have a matching license or notice in this directory
and a provenance entry in `../manifest.json`. dxpdf loads these files inside
the process only; it never installs them into the operating system.
